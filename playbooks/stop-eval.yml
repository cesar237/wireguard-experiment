---
- name: Kill all running processes 
  hosts: clients
  remote_user: "{{ username_client }}"
  gather_facts: false

  tasks:
    - name: Killall iperf3 processes
      shell:
        cmd: killall -9 iperf3
      ignore_errors: true
    - name: Killall netperf processes
      shell:
        cmd: killall -9 netperf
      ignore_errors: true